# CLAUDE.md

このファイルは Claude Code への引き継ぎ指示である．作業開始前に必ず全文を読むこと．

## プロジェクト概要

研究室のゼミ（中間発表・期末発表レビュー）で使う質疑支援Webアプリ．
聴衆がスマートフォンから匿名で質問を投稿し，質疑の場でそれを共感数順に消化する．
あわせて聴衆が理解を見失った時刻を記録し，発表全体の理解度の推移として可視化する．

詳細は `docs/requirements.md` を参照．画面設計は `docs/wireframe.html` をブラウザで開いて確認すること．

## 最重要の制約

**開発時間は5時間程度（半日ハッカソン）である．**
この制約が他の全ての判断に優先する．迷ったら，動くものを小さく作る側を選ぶこと．

- 未完成でも動く状態を常に保つ．大きなリファクタリングの途中で時間切れになる事態を避ける
- `docs/requirements.md` の優先度 Must / Should / Could を守る．Must が全て通るまで Should に着手しない
- 仕様に迷いが生じたら，勝手に決めずに人間に確認する

## 役割分担

- **Claude Code**：実装のほぼ全て
- **人間（渡嘉敷）**：実機での動作確認，見た目の判断，軌道修正

人間は React や TypeScript に不慣れである．ただし C#/WPF，Python，Java の経験があるため，
Python コードは読めるし修正もできる．**Python 側には日本語コメントを丁寧に入れること．**

## 技術構成（確定事項・変更禁止）

| 層 | 採用 | 理由 |
|---|---|---|
| サーバ | Python 3.11+ / FastAPI | 人間が読める．WebSocket が標準で扱える |
| 配信 | WebSocket（サーバ→クライアントの一方向） | 更新の即時反映 |
| データ | プロセス内メモリ（dataclass + dict） | ゼミ1回で完結する．DB は使わない |
| フロント | 素の HTML / CSS / JavaScript | **ビルドツールを一切使わない** |
| 並び替え演出 | View Transitions API | Baseline．5行で FLIP 相当が書ける |
| グラフ | SVG を直接生成 | 描画ライブラリ不要 |

### 使ってはいけないもの

- React，Vue，Svelte，その他のフロントエンドフレームワーク
- npm，Vite，webpack，その他のビルドツール．`package.json` を作らないこと
- Tailwind，Bootstrap，その他の CSS フレームワーク
- Chart.js，Recharts，D3 などの描画ライブラリ
- データベース（SQLite を含む）

ブラウザで `static/*.html` を直接開けば動く状態を維持する．
CDN からのスクリプト読み込みも行わない．オフラインでも動作すること．

### Python の依存

`requirements.txt` に記載のもののみ．追加が必要なら人間に確認する．

## 設計の要点

### 書き込みは REST，更新の配信は WebSocket

クライアントからサーバへの送信は全て HTTP POST で行う．
WebSocket はサーバからの一斉配信専用とし，クライアントからは何も送らない．
この分離を崩さないこと．再接続処理が単純になり，デバッグが容易になる．

メッセージ仕様は `docs/protocol.md` に定義済み．**勝手に変更しない．**

### 発表中は質問の本文を出さない

発表者ビュー（`/present`）は発表中，質問の件数のみを表示する．
本文を表示するのは質疑モードに入ってからである．これは意図的な仕様であり，
発表者が発表しながら批判的な質問を読んで動揺することを防ぐためのものである．

### 学年の丸め処理を必ず入れる

投稿には学年バッジ（B3 / B4 / M1 / M2 / D1 / 教員）を表示するが，
**その発表で投稿者が1名しかいない学年は「学生」に丸めて表示する．**
研究室は小規模であり，丸めがないと学年表示が実名と同じ意味を持ってしまう．
匿名性が壊れると質問が出なくなり，アプリの存在意義が失われる．

### 参加者の同一性

参加時にランダムなトークンを発行し，`localStorage` に保存する．
氏名・学籍番号・IPアドレスは一切保持しない．
トークンと投稿の対応はサーバ内部のみに留め，いかなるレスポンスにも含めない．

## ディレクトリ構成

```
seminar-qa/
├── CLAUDE.md                この文書
├── README.md                起動方法
├── requirements.txt
├── docs/
│   ├── requirements.md      要件定義書
│   ├── protocol.md          REST / WebSocket メッセージ仕様
│   ├── ui-guidelines.md     配色・フォント・アニメーション仕様
│   └── wireframe.html       画面ワイヤーフレーム
├── app/
│   ├── main.py              FastAPI エントリポイント．ルーティング
│   ├── models.py            dataclass 定義
│   ├── store.py             インメモリの状態保持と操作
│   ├── hub.py               WebSocket 接続管理とブロードキャスト
│   └── teams.py             Teams Workflows への投稿
├── static/
│   ├── join.html            参加（学年選択）
│   ├── live.html            参加者（発表中／質疑中の両方を担う）
│   ├── present.html         発表者ビュー
│   ├── review.html          質疑ビュー（スクリーン投影）
│   ├── css/
│   │   ├── tokens.css       設計トークン．色・余白・フォントはここに集約
│   │   └── app.css          共通スタイル
│   └── js/
│       ├── ws.js            WebSocket 接続と自動再接続
│       ├── api.js           REST 呼び出しの薄いラッパ
│       ├── join.js
│       ├── live.js
│       ├── present.js
│       ├── review.js
│       └── chart.js         SVG 折れ線の生成
└── tools/
    └── fake_audience.py     ダミー参加者を生成する検証用スクリプト
```

## 実装順序

上から順に実装する．各段階の終わりで人間が実機確認を行うため，
**その段階で動く状態にしてから次に進むこと．**

| 段階 | 実装内容 | 人間が確認すること |
|---|---|---|
| 0 | 疎通確認（後述） | スマホからサーバに繋がるか |
| 1 | `store.py`，`hub.py`，`main.py` の骨組み，WebSocket 配信 | 状態変更が全端末に届くか |
| 2 | `join.html`，`live.html`（わからん・質問投稿） | 投稿が溜まるか |
| 3 | `review.html`（一覧・共感・消化），View Transitions | 並び替えの気持ちよさ |
| 4 | `present.html`，発表の開始／終了／切替 | 2人分を連続で回せるか |
| 5 | `chart.js`（わからんグラフ） | 折れ線が読めるか |
| 6 | `teams.py`（Teams 投稿） | カードが届くか |
| 7 | リハーサル（最低30分確保） | 投影して文字が読めるか |

`tools/fake_audience.py` は段階1の直後に作ること．
人間は一人でテストするため，これがないと複数人前提の機能を確認できない．

## 着手前に必ず行う疎通確認

**この2つが通らないと設計を変える必要があるため，コードを書く前に確認する．**

1. **学内 Wi-Fi のクライアント間通信**
   サーバを起動し，人間のスマートフォンから `http://<サーバPCのIP>:8000` が開けるか．
   開けない場合はプライバシーセパレータが有効になっている．
   その場合はテザリング，またはトンネリングによる外部URL発行に切り替える．
   これは致命的な問題であり，先に潰しておかないと後半で全てが無駄になる．

2. **Teams の Webhook**
   Teams チャネルの Workflows から「Send webhook alerts to a channel」で
   フローを作成し，発行された URL に `curl` で1通 POST して届くか確認する．
   テナント設定で Power Automate が制限されている可能性がある．
   通らなければ Teams 連携（FR-13）を早期に切り捨てる判断を人間に求めること．

   従来の Office 365 コネクタ（Incoming Webhook）は 2026年5月に廃止済みである．
   ペイロードは MessageCard ではなく **Adaptive Card 形式**でなければならない．

## コーディング規約

- Python は型ヒントを付ける．クラスは `dataclass` を用いる
- Python のコメントは日本語で書く．人間が読んで修正できることを優先する
- JavaScript は ES2022 の素の構文．モジュールは `<script type="module">` で読み込む
- CSS の色・余白・フォントは必ず `tokens.css` のカスタムプロパティを参照する．
  各ファイルに直接 `#rrggbb` を書かないこと
- 1ファイルが300行を超えそうなら分割を検討する
- 例外処理は握りつぶさない．WebSocket の切断は再接続で回復させる

## 未決事項（人間に確認すること）

- サーバを設置する端末（研究室の常設PCか，進行役のPCか）
- セッションを作成する担当（進行役を置くか，最初の発表者が兼ねるか）
- 投稿先の Teams チャネル
- 学年の選択肢に研究生・留学生等を含めるか
