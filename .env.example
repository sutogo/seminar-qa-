# 本版では Teams への自動投稿を実装していない．
# この変数を設定しても投稿は行われない（記録は発表者ビューからコピーする）．
#
# 実装する場合は，Teams チャネルの「⋯ → ワークフロー」から
# 「Send webhook alerts to a channel」でフローを作成し，発行された URL を入れる．
# チャネルを新設する必要はない．
TEAMS_WEBHOOK_URL=

# 参加画面と発表者ビューに出る名前．省略すると「ゼミ」になる．
SEMINAR_TITLE=

# 記録に載る日付．省略すると起動日になる．
SEMINAR_DATE=
